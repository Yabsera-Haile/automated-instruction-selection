"""Training-free distribution audit for instruction-data selection methods.

This package layers on top of the RDS+ pipeline (`minimal_multitask/`) without
modifying it. See `audit/REPO_MAP.md` for how the underlying repo works and how
selection outputs map back to pool examples.
"""
