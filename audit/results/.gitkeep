# Audit run outputs + logs land here.
# NOTE: the repo's root .gitignore ignores `results/` globally, so this directory's
# contents are git-ignored by design. This placeholder is force-added to keep the
# folder present in the tree.
